## 1. 基线梳理与测试脚手架

- [x] 1.1 盘点 `src/modal/*` 现有弹窗并建立改造清单（危险确认类/普通确认类/输入类），明确每个弹窗的前置条件、触发入口与验收标准。
- [x] 1.2 在测试目录补充弹窗行为测试分组与命名约定，统一复用 `tests/mocks/obsidian.ts` 的 Mock/Stub 能力并补充必要注释，确保后续用例可维护。

## 2. 危险确认类弹窗改造（TDD）

- [x] 2.1 [TDD-红] 为删除类弹窗编写失败测试：覆盖危险语义、主次按钮区分、确认后执行/取消不执行、空态安全关闭，并在测试中注明目的与预期结果。
- [x] 2.2 [TDD-绿] 实现删除类弹窗的统一结构与危险语义，使 2.1 测试通过；为关键实现补充职责注释（结构分区、事件分支意图）。
- [x] 2.3 [TDD-重构] 收敛删除类弹窗中重复的渲染与提示逻辑，保持行为不变并确保测试持续通过。

## 3. 普通确认类弹窗改造（TDD）

- [x] 3.1 [TDD-红] 为移动/复制、创建、合并、同步等普通确认弹窗编写失败测试：覆盖统一摘要信息、长列表可操作、确认/取消反馈一致性。
- [x] 3.2 [TDD-绿] 实现普通确认类弹窗统一骨架（标题/说明/摘要/列表/操作区）与主次按钮语义，使 3.1 测试通过，并补充关键实现注释。
- [x] 3.3 [TDD-重构] 提炼普通确认类共享渲染片段或辅助方法，减少重复代码并保持测试稳定通过。

## 4. 输入类弹窗改造（TDD）

- [x] 4.1 [TDD-红] 为重命名、属性编辑等输入类弹窗编写失败测试：覆盖必填校验阻断、错误提示可操作性、取消不产生副作用。
- [x] 4.2 [TDD-绿] 实现输入类弹窗的统一布局与校验反馈规范，使 4.1 测试通过，并补充功能边界注释。
- [x] 4.3 [TDD-重构] 优化输入校验与提示文案复用策略，消除重复逻辑并保持行为一致。

## 5. 样式统一与主题兼容验证

- [x] 5.1 在 `styles.css` 中实现可复用的弹窗样式约定（结构区块、列表滚动、按钮主次、危险语义），并补充样式职责注释。
- [x] 5.2 完成深色/浅色主题下的人工验证清单：检查文本对比度、按钮可识别性、长列表可操作性；记录问题并修正。

## 6. 回归验证与交付

- [x] 6.1 运行并通过全部相关自动化测试与现有回归测试，确认命令触发链路在弹窗改造后无行为回退。
- [x] 6.2 按 capability 验收标准进行最终自检：统一结构、语义区分、空态安全、批量场景可用性全部满足后再提交实现结果。
