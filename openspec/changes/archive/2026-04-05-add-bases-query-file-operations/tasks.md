## 1. Bases 能力接入与命令注册

- [x] 1.1 梳理并确认 Bases 查询结果可访问入口（API/上下文对象），定义仅文件映射范围与失败反馈策略（验收：形成可执行读取约定与错误提示清单）。
- [x] 1.2 新增 Bases 命令组并完成全量命令注册（移动、同步 flomo、合并、删除、复制链接、属性编辑、重命名、添加文件到 Canvas、添加任务到 Canvas），命名风格与 Dataview 一致（验收：命令面板可检索且行为入口完整）。

## 2. Reader 读取与映射逻辑（TDD）

- [x] 2.1 [TDD-红] 编写 BasesReader 失败测试：无结果、全不可映射、混合结果、路径不存在、数据源不可用等场景；测试需补充注释说明测试目的、输入与预期结果（验收：测试先失败且覆盖主要边界）。
- [x] 2.2 [TDD-绿] 实现 BasesReader 最小逻辑：提取可映射文件、过滤不可映射项、统计 ignoredCount、输出提示；实现代码补充职责注释（验收：2.1 全部测试通过）。
- [x] 2.3 [TDD-重构] 重构 Reader 内部解析与判空流程，消除重复逻辑并稳定错误处理文案；保持测试绿灯（验收：代码结构更清晰且测试持续通过）。

## 3. 交互、安全与回归保障（TDD）

- [x] 3.1 [TDD-红] 编写命令集成测试：Bases 命令触发链路、危险操作确认弹窗、取消不执行、忽略数量提示可观察；测试代码补充简要注释（验收：新增测试在实现前失败）。
- [x] 3.2 [TDD-绿] 接入现有确认弹窗和 Action 执行链路，确保仅处理文件且 row 不被写入（验收：3.1 测试通过，满足“仅文件操作”约束）。
- [x] 3.3 执行并补充回归测试（Dataview/Search/CurrentFile 相关），确保既有能力无回归（验收：现有测试与新增测试全量通过）。

## 4. 文档与发布准备

- [x] 4.1 更新 README 与 README_zh：新增 Bases 使用说明、命令范围、忽略策略、非 row 操作边界（验收：文档可指导用户正确使用）。
- [x] 4.2 整理变更说明与验收记录：包含已覆盖场景、未覆盖风险与后续演进建议（验收：可用于发布评审与后续迭代输入）。
